/**
 * Base Environment
 *
 * This environment should be
 * used to extend all other
 * environments.
 */

import { IConfig } from 'facile/interfaces';

let baseConfig = {

	host: '127.0.0.1',
	port: 3000

};
